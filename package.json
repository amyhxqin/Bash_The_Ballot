{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for interacting with database",
  "main": "server.js",
  "engines": {
    "node": ">=6.9.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd frontend && npm run build && cd .. && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "mssql": "^4.1.0"
  }
}
